blank_issues_enabled: false
contact_links:
  - name: Security disclosure
    # Must match the canonical repository (Cargo.toml `repository`). The owner
    # is the org `Atlas-Inf`. A link to any previous home leaves a reporter
    # with nowhere to file.
    url: https://github.com/Atlas-Inf/atlas/security/advisories/new
    about: Report a vulnerability privately via GitHub Security Advisories. See SECURITY.md.

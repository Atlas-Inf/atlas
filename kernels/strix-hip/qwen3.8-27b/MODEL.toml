[model]
name = "qwen3.8-27b"
hf_id = "unsloth/Qwen3.8-27B-NVFP4"
params = "27B"
active_params = "27B"
architecture = "Hybrid Attention + GDN + Dense FFN + MRoPE"

# ── Why this target exists, and why it compiles no kernels of its own ──────
# VERIFIED 2026-08-18 on the Strix Halo box against the checkpoint actually on
# disk (unsloth/Qwen3.8-27B-NVFP4, snapshot 7d6f8d4d): every field of
# `text_config` is byte-identical to unsloth/Qwen3.6-27B-NVFP4's — layer
# counts, layer_types histogram, hidden/head/kv dims, vocab, rope_parameters,
# attn_output_gate, mtp_num_hidden_layers. The two checkpoints differ ONLY in
# weights and in `quantization_config`.
#
# Two consequences, both load-bearing:
#
#  * `kernel_source` reuses qwen3.6-27b's gfx1151 .cu tree verbatim rather
#    than vendoring a duplicate copy. There is no 3.8-specific kernel work on
#    this backend, exactly as on gb10.
#
#  * `match_names` is REQUIRED, not decorative. config.json cannot tell 3.8
#    from 3.6 — same model_type "qwen3_5", same hidden_size 5120, same every
#    numeric field — so kernel-target resolution hits an exact
#    (qwen3_5, 5120) tie and breaks it by matching these needles against the
#    checkpoint reference (HF id / --model-name / model dir). An unbroken tie
#    is a hard startup error, never a build-order pick. See
#    atlas-kernels/src/resolve.rs.
match_names = ["qwen3.8-27b"]
kernel_source = "qwen3.6-27b"

# Architecture read from the checkpoint's text_config, not copied.
layers_total = 64
layers_full_attention = 16
layers_linear_attention = 48

hidden_dim = 5120
head_dim = 256
q_heads = 24
kv_heads = 4
intermediate_size = 17408

vocab_size = 248320
max_position_embeddings = 262144
rope_theta = 10000000
attn_output_gate = true
partial_rotary_factor = 0.25

mrope_interleaved = true
mrope_section = [11, 11, 10]

# ── MTP: 1, and this is a DELIBERATE divergence from gb10/qwen3.8-27b ──────
# The gb10 target sets `mtp_layers = 0`. That is wrong for this checkpoint,
# and it matters more here than it does on gb10 because the Strix MLPerf-edge
# recipe serves with `--speculative --num-drafts N`, which is dead weight
# without an MTP head.
#
# Evidence, read off the snapshot on disk rather than inferred:
#   config.json  text_config.mtp_num_hidden_layers = 1
#   model.safetensors.index.json references model_mtp.safetensors, and the
#   weight_map carries 15 `mtp.*` entries — mtp.fc.weight,
#   mtp.layers.0.{input_layernorm,post_attention_layernorm,mlp.*,self_attn.*}
#
# So the head ships, it is indexed, and the qwen35 loader's `mtp.fc.weight`
# auto-detect finds it. This is the identical trap qwen3.6-27b/MODEL.toml
# documents ("was previously stalled at 0 — stale value, MTP silently
# disabled"): a 0 here produces no error and no warning, just a serve that
# quietly ignores --speculative and decodes at 1x.
mtp_layers = 1

# The checkpoint is a Qwen3_5ForConditionalGeneration with a populated
# vision_config; quantization_config.ignore lists the visual blocks.
vision = true

# Carried from the 27B siblings (3.5/3.6 share Atlas's hybrid-GDN
# long-context behaviour; 3.8 is the same architecture). UNMEASURED on 3.8
# weights on gfx1151 — revisit if long-context evals land.
effective_context = 24576

# ── Sampling ───────────────────────────────────────────────────────────────
# Taken from gb10/qwen3.8-27b, NOT from strix/qwen3.6-27b. Sampling is a
# property of the WEIGHTS, and these are the same weights gb10 serves, where
# the card values were A/B'd on this checkpoint (N=10/tier: thinking temp 1.0
# -> 4090 s Sigma-wall, temp 0.6 -> 4182 s, both 10/10 — the 3.6 retune bought
# nothing on 3.8 weights, and 3.6's degeneration mode was not reproduced).
# Importing qwen3.6-27b's empirically-retuned 0.6 / rep_pen 1.1 / dry 0.5
# would be importing a bisect run against DIFFERENT weights.
#
# [sampling.tools] stays penalty-free: penalties are applied BEFORE the
# grammar mask, so they shift the argmax among legal JSON tokens, and
# tool-call JSON legitimately repeats `"`, `{`, `,` and key names. On
# qwen3.6-27b that corrupted grammar-constrained args even at temp 0 and cost
# ~9 points of BFCL non_live until the 2026-07-04 penalty-free sync.
[sampling.thinking_text]
temperature = 1.0
top_p = 0.95
top_k = 20
min_p = 0.0
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.thinking_coding]
temperature = 1.0
top_p = 0.95
top_k = 20
min_p = 0.0
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.non_thinking]
temperature = 0.7
top_p = 0.8
top_k = 20
min_p = 0.0
presence_penalty = 1.5
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[sampling.tools]
temperature = 0.6
top_p = 0.95
top_k = 20
presence_penalty = 0.0
frequency_penalty = 0.0
repetition_penalty = 1.0
dry_multiplier = 0.0

[behavior]
# Both dense-27B siblings (3.5 and 3.6, independently trained) loop on
# long-code prompts, so the prose-attractor is treated as a family trait of
# this architecture until 3.8 evidence on gfx1151 says otherwise.
enable_loop_watchdog = true

# ── thinking_default: deliberately NOT set here ────────────────────────────
# gb10/qwen3.8-27b sets `thinking_default = true`. strix/qwen3.6-27b
# deliberately leaves it unset, because the dense 27B FP8/NVFP4 weights
# exhibited a post-`</think>` content-collapse ON gfx1151 specifically —
# coherent reasoning_content, then a premature stop plus chat-template
# artefacts on the transition out of thinking.
#
# That is a gfx1151 x weights interaction, and 3.8 is different weights on the
# same gfx1151. Neither sibling's value transfers on evidence: gb10's `true`
# was measured on different silicon, and 3.6's implicit `false` was measured
# on different weights. Unset (= false) is the conservative choice — clients
# still get coherent answers, and an explicit per-request enable_thinking=true
# still wins — and it is the value the 3.6 Strix submission was certified
# under. Flip it here only with a gfx1151 A/B on THESE weights recorded in
# this comment.
thinking_in_tools = false
max_thinking_budget = 512

# default_num_drafts: 3 (K=4 verify). gb10/qwen3.8-27b leaves this unset on
# the argument that draft acceptance is a weights property and 3.8 must earn
# its own K — earned: on gfx1151 the K=4 arm measures 28.3–28.6 tok/s vs
# 24.6 at K=1 (+~15%, mean_na 2.16–2.34 across harnesses), and the 995-row
# bfcl-subset golden draw scored 83.02/80.41 under it (run record
# run-1789370409958768995). The serve recipe may still pass --num-drafts
# explicitly, which overrides this.
default_num_drafts = 3

# default_kv_dtype: "bf16" — the proven configuration. gb10/qwen3.8-27b sets
# "fp8", carrying it with the shared kernel set; on gfx1151 the SCALE device
# FP8 encode path is the one this port works around (ATLAS_W4A16_VARIANT=v1 /
# ATLAS_NO_FP8_PREDEQUANT=1 exist for exactly that reason). This field must be
# set, not merely left unset: an empty default resolves to the ENGINE default
# fp8 (DEFAULT_KV_CACHE_DTYPE), which would silently put a bare `spark serve`
# on exactly the unproven path the recipe avoids. The certified MLPerf-edge
# run and the 2026-09-14 2.55h soak both served bf16.
default_kv_dtype = "bf16"

# Upstream reports model_type = "qwen3_5". The MRoPE -> "qwen3_6_moe"
# dispatch rewrite fires only for MoE checkpoints (is_moe gate in
# atlas-core config/dispatch.rs), and this is a DENSE hybrid, so it stays
# "qwen3_5". No belt-and-braces qwen3_6_moe entry: no 3.8 checkpoint is known
# to rewrite, and every extra colliding entry widens the ambiguity surface
# that resolution has to break.
[[model_types]]
model_type = "qwen3_5"
hidden_size = 5120

# ── DFlash2 speculative decoding ───────────────────────────────────────────
# A 3.8-trained drafter now exists — the strix-hip kernel tree carries the
# DFlash2 kernels (sink paged attention, batchm GEMV, dflash2 conv/candidate
# selector), so incoai/Qwen3.8-27B-DFlash2 is wired the same as on gb10.
# Serving defaults (Option-B paged propose, small-M GEMV arm, GPU_UTIL 0.80)
# live in serve-amd.sh, not here — see docs/porting/QWEN38_STRIX_PORT.md
# "2026-09-15 — DFlash2 speculative decoding on gfx1151".
[dflash]
draft_model = "incoai/Qwen3.8-27B-DFlash2"
gamma = 8
window_size = 4096
mask_token_id = 248070
target_layer_ids = [5, 19, 33, 47, 61]

# ── expected_absent ────────────────────────────────────────────────────────
# Harvested 2026-09-13 from the fingerprinted ST-995 serve (winbox, ROCm
# 10.0.0, spark.exe 367AA4E4 / B4C3E75D): 102 modules embedded, 87 probes
# unresolved, each a `try_kernel` handle-gated optional arm. Every entry
# below names the dispatch site and the resolved fallback it binds to;
# none are silenced speculatively — the full run (995/995, zero faults)
# was served through these fallbacks. strix-hip/qwen3.6-27b shares this
# kernel_source and needs the same audit when it is next exercised.
#
# STRUCTURAL RULE for this table: the fallback named in each reason is a
# kernel whose module the audit table shows RESOLVED ("used") on this
# target. Where an arm is unported rather than merely unselected, the
# reason says UNPORTED — those are candidate work items, not losses.

# ── dense FFN: alternative-quant arms (checkpoint is NVFP4 throughout) ─────
# Probed unconditionally at init (dense_ffn.rs); every arm below is selected
# only by weight dtype or an env opt-in, and the NVFP4/default arms all
# resolve. A 0 handle is dead code on this checkpoint.
[expected_absent.q4k_mmq]
atlas_q4k_mmq128_nc = "Q4_K MMQ gate/up prefill arm; ATLAS_FFN_MMQ env-gated AND requires q4k weights materialized from NVFP4 (dense_ffn.rs:466). Off in the recipe; w4a16 t_m128 serves prefill."
atlas_q4k_mmq128_wc = "Weight-column twin of the above; same gate, same fallback."
atlas_q8_1_quantize_ds4_bf16 = "Q8_1 activation requant for the Q4_K MMQ path; same ATLAS_FFN_MMQ gate."

[expected_absent.q4k_quantize]
q4k_quantize = "Q4_K weight repack for ATLAS_FFN_MMQ; env off, handle!=0 gate at dense_ffn.rs:466."

[expected_absent.dequant_gguf_bf16]
dequant_q2_0_gn_to_bf16 = "GGUF Q2_0 group dequant; fires only when packed-Q2 weights are installed (q2_weights.is_some() early-returns first, dense_ffn.rs:463). NVFP4 checkpoint."

[expected_absent.dequant_nvfp4_bf16]
dequant_nvfp4_to_bf16 = "NVFP4->BF16 weight dequant feeding ONLY the env-gated Q4_K hybrid/faith2 prefill arms (dense_ffn.rs:2124). All consumers are off; decode GEMV reads NVFP4 natively."

[expected_absent.q2_0_gemv_vec]
q2_0_gemv_vec = "Q2_0 GGUF decode GEMV; q2_weights is None on this checkpoint — dtype-gated dead handle."
q2_0_gemv_vec_batchm = "batched twin; same dtype gate."

[expected_absent.w4a4]
w4a4_gemm = "W4A4 prefill GEMM; ATLAS_ATTN_W4A4 opt-in, handle!=0 gate. Resolved w4a16 arms serve."
w4a4_gemm_mfast = "Same situation as gb10: mfast exists only in nemotron-labs-3-puzzle-75b-a9b's shadow; dispatch falls back to w4a4_gemm, which is itself absent here and env-gated off. UNMEASURED."

[expected_absent.w8a16_gemv_fused]
w8a16_gemv_dual = "fused gate/up W8A16 GEMV; W8A16 weights never materialize on an NVFP4 checkpoint (dtype-gated)."
w8a16_gemv_silu_input = "SiLU-input twin; same dtype gate."

[expected_absent.w8a16_gemm_pipelined]
w8a16_gemm_pipelined = "Pipelined W8A16 GEMM alternate probed at qwen3_ssm/init.rs:432; dtype-gated, resolved w8a16_gemm/w4a16 arms cover the shapes it would take."

[expected_absent.w8a16_gemm_t]
w8a16_gemm_t_pipelined = "Pipelined transposed W8A16 GEMM alternate (attn init.rs:178); dtype-gated; resolved w8a16_gemm_t serves."

# ── dense FFN: w4a16/nvfp4 alternative instantiations ──────────────────────
# Pipelined/bf16-projection/int8 twins of RESOLVED w4a16 arms. Each probes
# then falls back within the same dtype family.
[expected_absent.w4a16]
int8_gemm_faith2 = "W4A8 requant->int8 MMA prefill; ATLAS_INT8_PREFILL opt-in (dense_ffn.rs:2080). Lossy path, off; t_m128 serves."
int8_gemm_i32acc = "faith5 i32-acc twin; same env gate."
requant_w_nvfp4_int8 = "Weight requant for the faith2 path; dead unless ATLAS_INT8_PREFILL/faith2 fires."
requant_a_bf16_int8 = "Activation requant twin; same gate."
w4a16_gemm_t_m128_bf16 = "BF16-projection prefill twin of w4a16_gemm_t_m128 (resolved); prefill_weights gates on handle!=0 — same declaration gb10 carries."
w4a16_gemm_t_m128_bf16_v2 = "8-warp pipelined twin of the bf16 variant; same fallback."
w4a16_gemm_t_p3 = "3-deep weight-pipeline twin; tgemm_kernel falls back to w4a16_gemm_t (resolved)."
w4a16_gemm_t_k64_p3 = "k64 pipelined twin; k64_kernel falls back to w4a16_gemm_t_k64 (required, resolves)."
w4a16_gemm_t_k64_n64_p3 = "Narrow-N deep-K twin; k64_n64_kernel returns 0-handle when absent and dispatch gates before firing (qwen3.6-27b carries the same entry)."
fp8_gemm_t_row_scaled = "DFlash Phase-G row-scaled BF16xFP8 GEMM for the FP8 drafter path (dflash_head/from_weights.rs:239, try_kernel). Not in strix-hip's w4a16 module; the ATLAS_DFLASH_DRAFTER_FP8 gate then keeps the BF16 drafter. Observed as the only two unresolved lookups on the 2026-09-19 DFlash gamma=8 ST-995/ST-996 serves at main@da943853 (AzeezStrix), which ran through the fallback. UNPORTED candidate: drafter-propose bytes."
fp8_gemm_t_row_scaled_m16 = "Small-M (M<=16) twin used for the lm_head swap in the gamma loop (from_weights.rs:252); same gate, same BF16 fallback."

# ── dense FFN: vendored Blackwell NVFP4 W4A4 MMQ family — UNPORTED ─────────
# The FP4 tensor-core MMA prefill arm (llama-derived, block-scale). Default
# ON on gb10 — here every handle resolves 0, so fp4mmq_prefill is false and
# gate/up/down fall back to the resolved w4a16 t_m128 path. This IS a
# performance gap on Windows prefill, declared not hidden: porting this
# family (or an FP8 equivalent, cf. commit 8eedc21cb's n_m128 arm) is the
# tracked prefill lever.
[expected_absent.nvfp4_mmq]
atlas_nvfp4_mmq128_nc = "UNPORTED: Blackwell FP4 MMA prefill, gate/up (dense_ffn.rs:2090). handle!=0 composite gate -> w4a16 t_m128. Prefill-perf candidate."
atlas_nvfp4_mmq128_wc = "Same family, weight-column variant."
atlas_nvfp4_mmq16_nc = "Same family, M=16 tile."
atlas_nvfp4_mmq16_wc = "Same family."
atlas_nvfp4_mmq32_nc = "Same family, M=32 tile."
atlas_nvfp4_mmq32_wc = "Same family."
atlas_nvfp4_mmq64_nc = "Same family, M=64 tile."
atlas_nvfp4_mmq64_wc = "Same family."
atlas_nvfp4_quantize_bf16 = "Activation FP4 quantizer feeding the MMQ arm; dead without it."
atlas_nvfp4_repack = "Weight repack helper for the MMQ arm; dead without it."
atlas_nvfp4_scale_bf16 = "Block-scale producer; gates the down-proj arm (fp4mmq_down)."
atlas_nvfp4_silu_mul_quant = "Scaled-SiLU epilogue for the arm; dead without it."
atlas_nvfp4_silu_mul_scaled = "SiLU-mul variant for the arm; dead without it."

# ── GDN/SSM: alternative recurrence + verify instantiations ────────────────
# The serving spine is gated_delta_rule_fla::chunk_delta_h_vfused (logged
# 'GDN state spine:' at boot, resolved). Decode verify uses wy2/wy3/wy4
# (all resolve). Everything below is a variant arm gated on handle!=0.
[expected_absent.gated_delta_rule_fla]
gated_delta_rule_chunk_delta_h_ksplit = "Pre-vfused state spine; vfused resolves and is default (init.rs:237-256 logs the pick)."
gated_delta_rule_chunk_delta_h_tc_vblock = "Tensor-core vblock twin; unselected."
gated_delta_rule_chunk_delta_h_tma = "TMA-descriptor variant (sm90+); not applicable to gfx1151."

[expected_absent.gated_delta_rule_regresident]
gated_delta_rule_prefill_regresident = "Register-resident prefill spine alternate; persistent/chunk paths resolve."

[expected_absent.gated_delta_rule_snap]
gated_delta_rule_decode_f32_norm_snap = "FP32-norm snapshot-decode variant; the recipe's decode resolves the base gated_delta_rule_decode."
gated_delta_rule_decode_f32_strided_norm_snap = "Strided twin; same."

[expected_absent.gated_delta_rule_wy17]
gated_delta_rule_wy17 = "K=17 verify WY arm; --num-drafts 3 caps K<=4 — unreachable in this recipe (K>4 unsupported, see BENCH note)."

[expected_absent.gated_delta_rule_wyn]
gated_delta_rule_wy5 = "K=5 verify arm; unreachable at num-drafts 3 (K<=4)."
gated_delta_rule_wy6 = "K=6; same."
gated_delta_rule_wy7 = "K=7; same."
gated_delta_rule_wy8 = "K=8; same. wyn arms matter only if lever #3 (K<=8 speculation) is implemented."

[expected_absent.gated_delta_rule_wy2_resident]
gated_delta_rule_wy2_resident = "gb10-only register-resident wy2 twin; init comment: 'non-gb10 targets simply resolve 0 and keep the base wy2' (resolved)."

[expected_absent.gated_delta_rule_wy2_resident_f16]
gated_delta_rule_wy2_resident_f16 = "FP16 h-state twin of the above; additionally needs ATLAS_SSM_H_FP16 (off — preflight would refuse --speculative without twins)."

[expected_absent.gated_delta_rule_wy3_resident]
gated_delta_rule_wy3_resident = "gb10-only register-resident wy3 twin; base wy3 resolves."

[expected_absent.gated_delta_rule_wy3_resident_f16]
gated_delta_rule_wy3_resident_f16 = "FP16 h-state twin; ATLAS_SSM_H_FP16 off."

[expected_absent.gated_delta_rule_wy3_f16]
gated_delta_rule_wy3_f16 = "FP16 h-state wy3; ATLAS_SSM_H_FP16 only (off)."

[expected_absent.gated_delta_rule_wy4_f16]
gated_delta_rule_wy4_f16 = "FP16 h-state wy4; same flag, off."

[expected_absent.gated_delta_rule_wy_f16]
gated_delta_rule_wy2_f16 = "FP16 h-state wy2; same flag, off."

# UNPORTED fused verify chain — conv1d+gates+recurrence+norm in one launch.
# This is the kernel-level fix behind lever #5 in the port report; the
# unfused resolved chain serves today at ~3.1 ms/GDN-layer.
[expected_absent.gdn_verify_fused_k2]
gdn_verify_fused_conv_k2 = "UNPORTED: fused conv+WY K=2 verify; falls back to separate conv1d/recurrence launches (resolved)."
gdn_verify_fused_norm_k2 = "UNPORTED: fused norm epilogue twin; same fallback."

[expected_absent.gdn_verify_fused_conv_kn]
gdn_verify_fused_conv_kn = "UNPORTED: fused conv verify, generic-K; same unfused fallback."
gdn_verify_fused_conv_kn_batched = "UNPORTED: batched variant; same."

[expected_absent.gdn_verify_fused_conv_kn_f32]
gdn_verify_fused_conv_kn_f32 = "UNPORTED: f32 accumulation variant; same."

[expected_absent.ssm_h_dtype]
ssm_h_state_f16_to_f32 = "FP16 h-state converter; only under ATLAS_SSM_H_FP16 (off; preflight refuses the flag without the wy_f16 twins anyway)."
ssm_h_state_f32_to_f16 = "Same, reverse direction."

[expected_absent.dense_gemv_fp8w]
dense_gemv_fp8w = "M=1 FP8-weight GEMV alternate; GDN FP8 projections resolve through gemv_fp8w (compiled) — this dense_ variant is a redundant probe, handle!=0 gated."

[expected_absent.fp8_gemm_t_blockscaled]
fp8_gemm_t_blockscaled = "Block-scaled FP8 prefill GEMM alternate for GDN projections (init.rs:466); handle!=0 gate, resolved gemm path serves the FP8-weight shapes."

# ── attention: TurboQuant KV prefill family + fused rope + Hadamard ────────
# All require a turboKV KV-compression mode (env/config) that this recipe
# does not enable; the resolved prefill_paged_nvfp4 / prefill_paged arms
# serve attention prefill. wht_bf16 and tq_plus_innerq are the Hadamard /
# inner-quant machinery of the same feature family.
[expected_absent.prefill_paged_turbo2]
inferspark_prefill_paged_turbo2 = "Turbo2 K/V prefill; turboKV modes off (bf16 KV cache in recipe). Resolved prefill_paged_nvfp4 serves."

[expected_absent.prefill_paged_turbo3]
inferspark_prefill_paged_turbo3_64 = "Same family, BR=64."

[expected_absent.prefill_paged_turbo4]
inferspark_prefill_paged_turbo4 = "Same family."
inferspark_prefill_paged_turbo4_64 = "Same family, BR=64."

[expected_absent.prefill_paged_turbo8]
inferspark_prefill_paged_turbo8_64 = "Same family, 8-bit-V tile BR=64."

[expected_absent.prefill_paged_bf16k_turbo2v]
inferspark_prefill_paged_bf16k_turbo2v_64 = "BF16-K + Turbo2-V asym prefill; turboKV off."

[expected_absent.prefill_paged_bf16k_turbo3v]
inferspark_prefill_paged_bf16k_turbo3v_64 = "BF16-K + Turbo3-V; turboKV off."

[expected_absent.prefill_paged_bf16k_turbo4v]
inferspark_prefill_paged_bf16k_turbo4v_64 = "BF16-K + Turbo4-V; turboKV off."

[expected_absent.prefill_paged_fp8k_turbo2v]
inferspark_prefill_paged_fp8k_turbo2v_64 = "FP8-K + Turbo2-V; turboKV off."

[expected_absent.prefill_paged_fp8k_turbo3v]
inferspark_prefill_paged_fp8k_turbo3v_64 = "FP8-K + Turbo3-V; turboKV off."

[expected_absent.prefill_paged_fp8k_turbo4v]
inferspark_prefill_paged_fp8k_turbo4v_64 = "FP8-K + Turbo4-V; turboKV off."

[expected_absent.prefill_paged_turbo3k_turbo8v]
inferspark_prefill_paged_turbo3k_turbo8v_64 = "Both-sides Turbo3K+8V; turboKV off."

[expected_absent.prefill_paged_turbo4k_turbo3v]
inferspark_prefill_paged_turbo4k_turbo3v_64 = "Both-sides Turbo4K+3V; turboKV off."

[expected_absent.prefill_paged_turbo4k_turbo8v]
inferspark_prefill_paged_turbo4k_turbo8v_64 = "Both-sides Turbo4K+8V; turboKV off."

[expected_absent.fused_k_norm_rope_cache]
fused_k_norm_rope_cache_write_bf16 = "Fused K-norm+rope+cache-write; dispatch falls back to the separate norm/rope/reshape_and_cache kernels (all resolved)."
fused_k_norm_rope_mrope_cache_write_bf16 = "MRoPE twin; same unfused fallback."

[expected_absent.wht_bf16]
wht_bf16_inplace = "Hadamard transform for TurboQuant+ K path; turboKV off — dead handle."
wht_bf16_inplace_inv = "Inverse twin; same."

[expected_absent.tq_plus_innerq_apply]
tq_plus_innerq_apply_q = "TurboQuant+ inner-quant apply on Q; turboKV off."
tq_plus_innerq_apply_k = "K-side twin; same."

# ── misc optional features ─────────────────────────────────────────────────
[expected_absent.token_overlay]
embed_rowdiff_bf16 = "Routed-token overlay feature; every hook guards kernel.0==0 (token_overlay.rs:31) — feature unused on this dense model."
embed_overlay_routed_bf16 = "Same feature."
lmhead_overlay_routed_bf16 = "Same feature."
lmhead_overlay_routed_f32 = "Same feature."
